{
  "devDependencies": {
    "@commitlint/config-conventional": "^19.7.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
  "server/**/*.{ts,tsx}": [
    "eslint --fix --max-warnings=0",
    "tsc --noEmit"
  ],
  "client/**/*.{ts,tsx}": [
    "next lint --max-warnings=0",
    "tsc --noEmit"
  ]
},
  "scripts": {
    "prepare": "husky"
  },
  "dependencies": {
    "eslint": "^9.20.1"
  }
}
